{% extends "layout.html.twig" %}

{% block description %}
{% endblock %}

{% block title %}
Actualités
{% endblock %}

{% block main %}
<main id="actualites">
    <section>
        <h2>Actualités</h2>
        <div class="actualities-container">
            {% for actuality in actualities %}
            <a class="actuality-card" href="index.php?route=actualite&id={{ actuality.id }}">
                <img src='{{ actuality.media.url }}');" alt="{{ actuality.media.alt }}" "class="actuality-card">
                <h3>{{ actuality.title }}</h3>
            </a>
            {% endfor %}
        </div>
    </section>
</main>
{% endblock %}
