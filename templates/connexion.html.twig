{% extends "layout.html.twig" %}

{% block description %}
{% endblock %}

{% block title %}
Se connecter
{% endblock %}

{% block main %}
<main id="connexion">
    <section>
        <h2>Se connecter</h2>
        <p>Cette page est réservée aux artistes et professionnels du spectacle. Pour accéder y accéder, veuillez vous connecter avec votre compte.</p>
        <p>Si vous n'avez pas encore de compte, n'hésitez pas à cliquer sur Créer un compte pour vous inscrire.</p>
        {% if  session.error_message is not null %}
            <div class="alert">
                <p>{{ session.error_message|striptags|striptags }}</p>
                <i class="fa-solid fa-xmark"></i>
            </div>
        {% endif %}

        <form action="index.php?route=check-login" method="post">
            <input type="hidden" name="csrf-token" id="csrf-token" value="{{ session.csrf_token }}" />

                <label for="email">Adresse mail</label>
                <input type="email" name="email" id="email"/>

                <label for="password">Mot de passe</label>
                <input type="password" name="password" id="password" />

                <button type="submit" class="submit-button">Se connecter</button>

        </form>
        <a href="">Mot de passe oublié ?</a>
        <a href="index.php?route=inscription">Créer un compte</a>
    </section>
</main>


{% endblock %}